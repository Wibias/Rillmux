name: Apply Streaming Decomposition (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use apply-streaming-decomposition-v2.yml"
